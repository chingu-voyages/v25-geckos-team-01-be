
const pushUpdate = (item, model) => {
    // This is assuming that the item is a String split by ","
    const items = item.split(",")
    // this is where you would look through each item and push it to the array
    // does not matter if the 
}